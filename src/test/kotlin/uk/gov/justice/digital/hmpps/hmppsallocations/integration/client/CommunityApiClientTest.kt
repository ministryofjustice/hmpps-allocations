package uk.gov.justice.digital.hmpps.hmppsallocations.integration.client

internal class CommunityApiClientTest {

/*
  @Autowired
  private val mockRestServiceServer: MockRestServiceServer? = null


  @Test
  fun `collect sentences from community api`(){
    mockRestServiceServer.expect(MockRestRequestMatchers.requestTo("/sdfsdf")).andExpect().andRespond(
      MockRestResponseCreators.withSuccess(someJson, MediaType.APPLICATION_JSON))
  }*/
}
